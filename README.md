# lunch2confluence

A small Python project that fetches lunch menus from selected restaurants in Vienna and prepares them for publishing to Confluence Cloud.

## Currently supported restaurants

- Wrenkh
- Wienerin

## Setup

```bash
python -m venv .venv
source .venv/Scripts/activate
python -m pip install -r requirements.txt